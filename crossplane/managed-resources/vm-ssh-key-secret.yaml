apiVersion: v1
kind: Secret
metadata:
  name: vm-ssh-public-key
  namespace: crossplane-system
type: Opaque
stringData:
  # IMPORTANTE: Substitua pela sua chave SSH pública real
  # Para gerar: ssh-keygen -t rsa -b 4096 -f ~/.ssh/id_rsa_crossplane
  # Depois copie o conteúdo de ~/.ssh/id_rsa_crossplane.pub para a linha abaixo
  publicKey: "ssh-rsa AAAAB3NzaC1yc2EAAAADAQABAAACAQD... SUBSTITUA_POR_SUA_CHAVE_PUBLICA"
